 
    import '../repositories/repositories.dart';

    class GetMovie_detailUseCase {
        final Movie_detailRepository repository;
      
        GetMovie_detailUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      